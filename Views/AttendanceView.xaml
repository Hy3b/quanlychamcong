<UserControl x:Class="QuanLyChamCong.Views.AttendanceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="700" d:DesignWidth="1000" FontFamily="Segoe UI">

    <UserControl.Resources>
        <Style TargetType="DataGridColumnHeader">
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
        </Style>
    </UserControl.Resources>

    <Grid Background="#F0F2F5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#FFFFFF" Padding="20" Margin="0,0,0,2">
            <StackPanel>
                <TextBlock Text="QUẢN LÝ THÔNG TIN CHẤM CÔNG" 
                           FontSize="24" FontWeight="Bold" Foreground="#1A237E"/>
                <TextBlock Text="Theo dõi thời gian làm việc toàn hệ thống" 
                           FontSize="14" Foreground="#757575" Margin="0,5,0,0"/>
            </StackPanel>
        </Border>
        
        <Border Grid.Row="1" Background="#FFFFFF" Margin="15,15,15,0" CornerRadius="8" Padding="15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <DatePicker Width="120"
                            VerticalContentAlignment="Center"
                            BorderThickness="0"
                            Background="Transparent"
                            SelectedDateFormat="Short">
                    <DatePicker.SelectedDate>
                            <Binding Path="SelectedDate" 
                                     Mode="TwoWay" 
                                     UpdateSourceTrigger="LostFocus"/>
                    </DatePicker.SelectedDate>
                </DatePicker>

                <TextBox Grid.Column="3" Width="250" Height="35" 
                         Text="{Binding SearchKeyword, UpdateSourceTrigger=PropertyChanged}"
                         VerticalContentAlignment="Center" Padding="5,0"
                         Margin="10,0"/>

                


                <TextBlock Grid.Column="3" ToolTip="Tìm theo tên hoặc ID..." 
                           IsHitTestVisible="False" VerticalAlignment="Center" Margin="18,0,0,0" Foreground="#CCC">
                 <Path Data="M15.5 14h-.79l-.28-.27A6.471 6.471 0 0 0 16 9.5 6.5 6.5 0 1 0 9.5 16c1.61 0 3.09-.59 4.23-1.57l.27.28v.79l5 4.99L20.49 19l-4.99-5zm-6 0C7.01 14 5 11.99 5 9.5S7.01 5 9.5 5 14 7.01 14 9.5 11.99 14 9.5 14z" 
                  Fill="#9CA3AF" Width="16" Height="16" HorizontalAlignment="Left" Margin="10,0,0,0" Stretch="Uniform" RenderTransformOrigin="28.812,0.81"/>
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding SearchKeyword}" Value="">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding SearchKeyword, Converter={x:Null}}" Value="False">
                                    <Setter Property="Visibility" Value="Hidden"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>

                <StackPanel Grid.Column="4" Orientation="Horizontal">

                    <Button Content="Xem lịch sử" 
                            Command="{Binding ViewAttendanceHistoryCommand}"
                            CommandParameter="{Binding ElementName=dgChamCong, Path=SelectedItem}"
                            Background="#FF9800" Foreground="White" FontWeight="Bold"
                            Width="120" Height="35" BorderThickness="0" Margin="0,0,10,0">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="4"/>
                            </Style>
                        </Button.Resources>

                    </Button>

                    <Button Content="Làm mới" Command="{Binding RefreshCommand}"
                            Background="#2196F3" Foreground="White" FontWeight="Bold"
                            Width="100" Height="35" BorderThickness="0">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="4"/>
                            </Style>
                        </Button.Resources>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Row="2" 
        Background="White"
        CornerRadius="12"
        BorderBrush="#E3E3E3"
        BorderThickness="1"
        Padding="15"
        Margin="15">

            <DataGrid ItemsSource="{Binding DailyAttendance}"
              x:Name="dgChamCong"
              AutoGenerateColumns="False"
              HeadersVisibility="Column"
              RowHeight="38"
              ColumnHeaderHeight="35"
              GridLinesVisibility="None"
              Background="White"
              BorderThickness="0"
              SelectionMode="Single"
              CanUserSortColumns="True"
              CanUserResizeRows="False"
              CanUserResizeColumns="False"
              IsReadOnly="True"
              ScrollViewer.CanContentScroll="True" 
              ScrollViewer.VerticalScrollBarVisibility="Auto">

                <DataGrid.ColumnHeaderStyle>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#F8F8F8"/>
                        <Setter Property="FontWeight" Value="SemiBold"/>
                        <Setter Property="BorderBrush" Value="#E0E0E0"/>
                        <Setter Property="BorderThickness" Value="0,0,0,1"/>
                        <Setter Property="Foreground" Value="#333"/>
                    </Style>
                </DataGrid.ColumnHeaderStyle>

                <DataGrid.Columns>
                    <DataGridTextColumn Header="Nhân viên ID" Binding="{Binding NhanVienId}" Width="*" FontWeight="SemiBold"/>
                    <DataGridTextColumn Header="Họ và Tên" Binding="{Binding HoTen}" Width="2*"/>
                    <DataGridTextColumn Header="Chức vụ" Binding="{Binding ChucVu}" Width="1.5*"/>
                    <DataGridTextColumn Header="Giờ bắt đầu" Binding="{Binding GioBatDau}" Width="*"/>
                    <DataGridTextColumn Header="Giờ kết thúc" Binding="{Binding GioKetThuc}" Width="*"/>
                    <DataGridTextColumn Header="Giờ vào" Binding="{Binding GioVao}" Width="*"/>
                    <DataGridTextColumn Header="Giờ ra" Binding="{Binding GioRa}" Width="*"/>
                    <DataGridTextColumn Header="Trạng thái" Binding="{Binding TrangThai}" Width="*"/>
                    
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>