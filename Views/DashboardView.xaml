<UserControl x:Class="QuanLyChamCong.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"   
             xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:QuanLyChamCong.Views"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1000"
             Background="#FFF5F7FA">
    <UserControl.Resources>
        <Style x:Key="StatCardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Margin" Value="0,0,15,0"/>
        </Style>

        <Style x:Key="StatIconStyle" TargetType="Border">
            <Setter Property="Width" Value="45"/>
            <Setter Property="Height" Value="45"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="VerticalAlignment" Value="Top"/>
        </Style>

        <Style x:Key="ChartCardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="20"/>
        </Style>

        <Style x:Key="ChartPlaceholderStyle" TargetType="Border">
            <Setter Property="Background" Value="#F9F9F9"/>
            <Setter Property="Margin" Value="0,15,0,0"/>
            <Setter Property="CornerRadius" Value="5"/>
        </Style>
    </UserControl.Resources>
    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <Grid Margin="25">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="#FFFFFF" Padding="20" Margin="0,0,0,2">
                <StackPanel>
                    <TextBlock Text="Trang Chủ" 
                           FontSize="24" FontWeight="Bold" Foreground="#1A237E"/>
                    <TextBlock Text="Hãy ngày hôm nay có gì mới!" 
                           FontSize="14" Foreground="#757575" Margin="0,5,0,0"/>
                </StackPanel>
            </Border>

            <UniformGrid Grid.Row="1" Columns="4" Rows="1" Margin="0,0,0,20">

                <Border Style="{DynamicResource StatCardStyle}">
                    <Grid>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="Tổng số nhân viên" FontSize="14" Foreground="Gray"/>
                            <TextBlock Text="{Binding SoLuongNhanVien}" FontSize="28" FontWeight="Bold" Margin="0,5,0,5"/>
                            
                        </StackPanel>
                        <Border Style="{DynamicResource StatIconStyle}" Background="#007BFF" HorizontalAlignment="Right">
                            <TextBlock Text="&#xE716;" FontFamily="Segoe MDL2 Assets" Foreground="White" FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </Grid>
                </Border>

                <Border Style="{DynamicResource StatCardStyle}">
                    <Grid>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="Có mặt hôm nay" FontSize="14" Foreground="Gray"/>
                            <TextBlock Text="{Binding SoLuongNhanVienDaChamCongHomNay}" FontSize="28" FontWeight="Bold" Margin="0,5,0,5"/>
                            <TextBlock Text="{Binding SoLuongNhanVienChuaCham}" FontSize="12" Foreground="Gray"/>
                        </StackPanel>
                        <Border Style="{DynamicResource StatIconStyle}" Background="#28a745" HorizontalAlignment="Right">
                            <TextBlock Text="&#xE73E;" FontFamily="Segoe MDL2 Assets" Foreground="White" FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </Grid>
                </Border>


            </UniformGrid>

            <Grid Grid.Row="2" Margin="0,0,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Style="{DynamicResource ChartCardStyle}" Margin="0,0,15,0">
                    <StackPanel>
                        <TextBlock Text="Xu hướng chuyên cần hàng tuần" FontSize="16" FontWeight="SemiBold"/>
                        <Border Style="{DynamicResource ChartPlaceholderStyle}" Height="250">
                            <lvc:CartesianChart Series="{Binding WeeklySeries}" LegendLocation="Bottom">
                                <lvc:CartesianChart.AxisX>
                                    <lvc:Axis Labels="{Binding Weeks}">
                                        <lvc:Axis.Separator>
                                            <lvc:Separator Step="1" StrokeThickness="0"/>
                                        </lvc:Axis.Separator>
                                    </lvc:Axis>
                                </lvc:CartesianChart.AxisX>
                            </lvc:CartesianChart>

                        </Border>
                    </StackPanel>
                </Border>

                <Border Grid.Column="1" Style="{DynamicResource ChartCardStyle}">
                    <StackPanel>
                        <TextBlock Text="Tình hình chấm công hôm nay" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,10"/>
                        <Border Style="{DynamicResource ChartPlaceholderStyle}" Height="250">
                            <lvc:PieChart  LegendLocation="Right" InnerRadius="0"
                                           Series="{Binding ChamCongHomNay}">

                            </lvc:PieChart>
                        </Border>
                    </StackPanel>
                </Border>
            </Grid>

            <Border Grid.Row="3" Style="{DynamicResource ChartCardStyle}" Margin="0,0,0,20">
                <StackPanel>
                    <TextBlock Text="So sánh chuyên cần hàng ngày" FontSize="16" FontWeight="SemiBold"/>
                    <Border Style="{DynamicResource ChartPlaceholderStyle}" Height="155">
                        <lvc:CartesianChart Series="{Binding DaylySeries}" LegendLocation="Bottom">
                            <lvc:CartesianChart.AxisX>
                                <lvc:Axis Title="Thời gian" Labels="{Binding Labels}">
                                    <lvc:Axis.Separator>
                                        <lvc:Separator Step="1" />
                                    </lvc:Axis.Separator>
                                </lvc:Axis>
                            </lvc:CartesianChart.AxisX>
                            <lvc:CartesianChart.AxisY>
                                <lvc:Axis Title="Số công chấm" Labels="{Binding Formatter}">
                                </lvc:Axis>
                            </lvc:CartesianChart.AxisY>
                        </lvc:CartesianChart>
                    </Border>
                </StackPanel>
            </Border>
        </Grid>
    </ScrollViewer>
</UserControl>
