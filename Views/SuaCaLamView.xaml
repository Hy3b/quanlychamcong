<Window x:Class="QuanLyChamCong.Views.SuaCaLamView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:QuanLyChamCong.Views"
        mc:Ignorable="d"
Title="Cập Nhật Ca Làm Việc" Height="500" Width="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="Ngày làm việc:" FontWeight="Bold" Margin="0,0,0,5"/>
            <DatePicker SelectedDate="{Binding CaCanSua.NgayLam}" 
                        HorizontalAlignment="Stretch" Margin="0,0,0,10"/>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="10"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="Giờ bắt đầu:" FontWeight="Bold" Margin="0,0,0,5"/>
                    <TextBox Text="{Binding CaCanSua.GioBatDau}" Padding="5"/>
                </StackPanel>

                <StackPanel Grid.Column="2">
                    <TextBlock Text="Giờ kết thúc:" FontWeight="Bold" Margin="0,0,0,5"/>
                    <TextBox Text="{Binding CaCanSua.GioKetThuc}" Padding="5"/>
                </StackPanel>
            </Grid>
        </StackPanel>

        <TextBlock Grid.Row="1" Text="Phân công nhân viên:" FontWeight="Bold" Margin="0,0,0,5"/>

        <Border Grid.Row="2" BorderBrush="Gray" BorderThickness="1" Margin="0,0,0,20">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding DanhSachNhanVien}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Padding="10" BorderBrush="#EEEEEE" BorderThickness="0,0,0,1">
                                <CheckBox IsChecked="{Binding IsSelected}" 
                                          Content="{Binding HoTen}"
                                          FontSize="14"
                                          VerticalContentAlignment="Center"/>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Hủy Bỏ" 
                    Width="100" Height="35" Margin="0,0,10,0"
                    Click="BtnHuy_Click"/>

            <Button Content="Lưu Thay Đổi" 
                    Width="120" Height="35"
                    Command="{Binding LuuThayDoiCommand}"
                    CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"/>
        </StackPanel>
    </Grid>
</Window>
